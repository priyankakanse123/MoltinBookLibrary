# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MoltinBooksLibrary' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

    pod 'Moltin', '~> 3.1'

  target 'MoltinBooksLibraryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MoltinBooksLibraryUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
